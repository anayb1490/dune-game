// PostCSS processes CSS files. Tailwind runs as a PostCSS plugin,
// and autoprefixer adds vendor prefixes for browser compatibility.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
